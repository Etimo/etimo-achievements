import React from 'react';
import './App.css';
import Router from './Router';

const App = (): JSX.Element => {
  return (
    <React.StrictMode>
      <Router />
    </React.StrictMode>
  );
};

export default App;
