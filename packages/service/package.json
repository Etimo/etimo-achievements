{
  "name": "@etimo-achievements/service",
  "version": "1.0.0",
  "description": "Service-related functions",
  "author": "Rickard Löfberg <rickard.lofberg@etimo.se>",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "clear": "npm run clean && rimraf node_modules",
    "compile": "tsc --build",
    "test-integration": "jest --preset ts-jest --env node --testMatch \"**/src/**/*.spec.ts\" --modulePathIgnorePatterns dist",
    "test": "exit 0; jest --preset ts-jest --env node --testMatch \"**/src/**/*.test.ts\" --modulePathIgnorePatterns dist"
  },
  "dependencies": {
    "@etimo-achievements/common": "^1.0.0",
    "@etimo-achievements/data": "^1.0.0",
    "node-fetch": "^2.6.5"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/node": "^16.10.2",
    "@types/node-fetch": "^2.5.12",
    "@types/secure-random-password": "^0.2.1",
    "jest": "^27.2.5",
    "ts-jest": "^27.0.6",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  }
}
