{
  "name": "@etimo-achievements/service",
  "version": "1.0.0",
  "description": "Service-related functions",
  "author": "Rickard Löfberg <rickard.lofberg@etimo.se>",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf dist *.tsbuildinfo",
    "clear": "npm run clean && rm -rf node_modules",
    "compile": "tsc --project tsconfig.build.json",
    "compile-test": "tsc --build",
    "test-integration": "jest --testMatch '**/src/**/*.spec.ts' --detectOpenHandles",
    "test": "exit 0; jest --testMatch '**/src/**/*.test.ts'"
  },
  "dependencies": {
    "@etimo-achievements/common": "^1.0.0",
    "@etimo-achievements/data": "^1.0.0",
    "node-fetch": "^2.6.5"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/node": "^16.10.2",
    "@types/node-fetch": "^2.5.12",
    "jest": "^27.2.5",
    "ts-jest": "^27.0.6",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  }
}
