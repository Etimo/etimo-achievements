{
  "name": "@etimo-achievements/data",
  "version": "1.0.0",
  "description": "Database entities & repositories",
  "author": "Niclas Lindstedt <niclas.lindstedt@etimo.se>",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "clear": "rimraf node_modules",
    "compile": "tsc --build && cp package.json dist",
    "migrate": "knex --knexfile src/config/knexfile.ts migrate:latest",
    "new-migration": "knex --knexfile src/config/knexfile.ts migrate:make",
    "new-seed": "knex --knexfile src/config/knexfile.ts seed:make",
    "seed": "knex --knexfile src/config/knexfile.ts seed:run",
    "test": "exit 0"
  },
  "dependencies": {
    "@etimo-achievements/common": "^1.0.0",
    "@etimo-achievements/security": "^1.0.0",
    "knex": "~0.21.21",
    "objection": "^2.2.16",
    "pg": "^8.7.1",
    "spacetime": "^6.16.3"
  },
  "devDependencies": {
    "@types/node": "^16.10.2",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  }
}
